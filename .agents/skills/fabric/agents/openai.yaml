interface:
  display_name: "Fabric"
  short_description: "Repo-specific patterns and workflows for fabric"
  default_prompt: "Use the fabric repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true